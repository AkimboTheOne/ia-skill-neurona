---
created: 2026-05-16T00:00:00-05:00
type: brief
status: published
source: generated
source_file:
  - docs/03-BRIEFS/20260515-160000-mini-proyecto-gobernanza-multi-instancia-y-boveda-descendiente.md
  - docs/05-NEURONA/gobernanza-multi-instancia-y-boveda-descendiente.md
tags:
  - brief
  - release
  - mem
  - ask
  - governance
aliases:
  - Release note: ask MVP and multi-instance governance
---

# Brief: release de `ask` MVP y gobernanza multi-instancia

## ONE THING

`$mem` ya expone `ask` como MVP de consulta coordinada y deja estabilizada la gobernanza multi-instancia con `docs/` como bóveda descendiente por defecto.

## PROOF

La CLI ahora responde `ask` por etapas con coincidencia heurística; `README.md`, `SKILL.md` y `llms.txt` lo exponen como superficie pública; y la red neuronal capitalizó el patrón en capturas, conexión y neuronas.

## READER TRANSFORMATION

El lector entiende que la consulta de memoria ya no depende sólo de navegación manual: puede reabrir stages, recuperar evidencia y alimentar razonamiento sin confundir la bóveda con el contrato del skill.

## THREE HOOKS

1. La memoria ya no sólo almacena: también se pregunta.
2. `docs/` es bóveda viva, no raíz accidental.
3. `ask` es pequeño, pero suficiente para activar la red.

## THREE CLOSERS

1. El MVP funciona cuando la consulta deja de ser una idea y se vuelve un comando útil.
2. La gobernanza importa porque mantiene separadas la forma del skill y la memoria del proyecto.
3. La próxima mejora debe sumar capacidad, no volver a discutir la frontera base.
