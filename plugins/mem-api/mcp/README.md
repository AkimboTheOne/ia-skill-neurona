# MCP future adapter

Este directorio reserva el contrato para la futura superficie MCP del servicio `mem-api`.

La primera iteración no implementa el adaptador, pero deja explícito que:

- la bóveda se configura por instancia;
- FastAPI es la fachada inicial;
- MCP se montará sobre la misma base cuando el contrato madure.

